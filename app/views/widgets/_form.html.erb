<%= form_for(@widget) do |f| %>
  <% if @widget.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@widget.errors.count, "error") %> prohibited this widget from being saved:</h2>

      <ul>
      <% @widget.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :email_id %><br />
    <%= f.text_field :email_id, :style => 'width:99%' %>
  </div>
  <div class="field">
    <%= f.label :name %><br />
    <%= f.text_field :name,:style => 'width:99%'  %>
  </div>
  <div class="field">
    <%= f.label :message %><br />
    <%= f.text_area :message, :rows => 3,:style => 'width:99%'  %>
  </div>
  <div class="actions">
    <%= f.submit "Submit" %>
  </div>
<% end %>
